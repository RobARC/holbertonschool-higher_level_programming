#!/usr/bin/python3
def replace_in_list(my_list, idx, element):
    lenght = len(my_list)
    my_list[idx] = element
    if idx < 0:
        return my_lyst
    elif idx > lenght:
        return my_list
    return(my_list)

